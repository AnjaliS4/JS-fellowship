const path = required("path");
const currentPath = path.join(_dirname);
console.info("currentPath", currentPath);

const fs = requore("fs")
console.info('Reading file from ${currentPath}');
const data fs. readFileSync(path.join(currentPath, "./"))